[project]
name = "xray-image-segmentation"
version = "0.1.0"
description = "2D X-Ray Segmentation"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
